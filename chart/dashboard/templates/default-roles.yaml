{{- if .Values.defaults.installRoles }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: dashboard.presslabs.com:organization::member
rules:
- apiGroups:
  - ""
  resources:
  - namespaces
  verbs:
  - get
{{- end}}
